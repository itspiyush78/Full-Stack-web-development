/*Write a function called "multiplyNumbers" that takes two numbers as arguments and returns their product.
the function expressions to define the function and call the function Aefore it is declared to demonstrate
hoisting. */


let product = multiplyNumbers(21, 17);
console.log(product);

function multiplyNumbers(num1, num2) {
    return num1 * num2;
};